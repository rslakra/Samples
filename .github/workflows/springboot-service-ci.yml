name: SpringBoot - SpringbootService CI

on:
  push:
    paths:
      - "SpringBoot/springboot-service/**"
      - ".github/workflows/springboot-service-ci.yml"
  pull_request:
    paths:
      - "SpringBoot/springboot-service/**"
      - ".github/workflows/springboot-service-ci.yml"
  workflow_dispatch:

defaults:
  run:
    working-directory: SpringBoot/springboot-service

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
          cache-dependency-path: SpringBoot/springboot-service/pom.xml

      - name: Build and run unit tests
        run: mvn -B -ntp test
