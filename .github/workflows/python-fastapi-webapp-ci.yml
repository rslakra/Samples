name: Python - FastAPI WebApp CI

on:
  push:
    paths:
      - "Python/FastAPIWebApp/**"
      - ".github/workflows/fastapi-webapp-ci.yml"
  pull_request:
    paths:
      - "Python/FastAPIWebApp/**"
      - ".github/workflows/fastapi-webapp-ci.yml"
  workflow_dispatch:

defaults:
  run:
    working-directory: Python/FastAPIWebApp

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: Python/FastAPIWebApp/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run unit tests
        run: python -m unittest discover -s tests -p "test_*.py" -t . -v
