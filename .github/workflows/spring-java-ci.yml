name: Spring - SpringJava CI

on:
  push:
    paths:
      - "Spring/spring-java/**"
      - ".github/workflows/spring-java-ci.yml"
  pull_request:
    paths:
      - "Spring/spring-java/**"
      - ".github/workflows/spring-java-ci.yml"
  workflow_dispatch:

defaults:
  run:
    working-directory: Spring/spring-java

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
          cache-dependency-path: Spring/spring-java/pom.xml

      - name: Build and run unit tests
        run: mvn -B -ntp test
