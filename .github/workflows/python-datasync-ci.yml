name: Python - DataSync CI

on:
  push:
    paths:
      - "Python/DataSync/**"
      - ".github/workflows/python-datasync-ci.yml"
  pull_request:
    paths:
      - "Python/DataSync/**"
      - ".github/workflows/python-datasync-ci.yml"
  workflow_dispatch:

defaults:
  run:
    working-directory: Python/DataSync

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: Python/DataSync/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run unit tests
        run: python -m unittest discover -s tests -p "test_*.py" -t . -v
